{.repeated section items}
<div class="grid-item">
    <div class="thumbnail">
        <img src="{assetUrl}" alt="{title}" />
        <div class="overlay">
            <a href="{fullUrl}" class="button large fade-location">{title}</a>
        </div>
    </div>
</div>
{.end}
